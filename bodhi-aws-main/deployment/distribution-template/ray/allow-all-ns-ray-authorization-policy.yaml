apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: kubeflow-allow-ray
  namespace: ray
spec:
  action: ALLOW
  rules:
    - {}
    # - from:
    #     - source:
    #         namespaces:
    #           - mlflow
