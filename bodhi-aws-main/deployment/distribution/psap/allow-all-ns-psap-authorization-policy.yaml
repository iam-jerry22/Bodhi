apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: kubeflow-allow-psap
  namespace: psap
spec:
  action: ALLOW
  rules:
    - {}
    # - from:
    #     - source:
    #         namespaces:
    #           - psap
